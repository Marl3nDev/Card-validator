import './style.css';
import { initializeCardValidator } from './dom.js';

document.addEventListener('DOMContentLoaded', initializeCardValidator);

window.cardValidator = {
  initializeCardValidator
};